{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://example.org/schemas/membership/user.removed.schema.json",
  "title": "Membership User Removed Payload",
  "description": "Payload schema for an event emitted when a user is removed from a domain entity.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "resource",
    "user",
    "removedTimestamp"
  ],
  "properties": {
    "resource": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "kind",
        "id",
        "name"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "minLength": 1
        },
        "id": {
          "type": "string",
          "minLength": 1
        },
        "name": {
          "type": "string",
          "minLength": 1
        }
      },
      "description": "Domain entity that lost a user membership."
    },
    "user": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id"
      ],
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1
        },
        "login": {
          "type": [
            "string",
            "null"
          ]
        },
        "displayName": {
          "type": [
            "string",
            "null"
          ]
        },
        "email": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "description": "User that was removed."
    },
    "previousRole": {
      "type": [
        "string",
        "null"
      ],
      "description": "Role held before removal, if available."
    },
    "reason": {
      "type": [
        "string",
        "null"
      ],
      "description": "Optional provider or policy reason for removal."
    },
    "removedBy": {
      "type": [
        "string",
        "null"
      ],
      "description": "Actor or service principal that initiated removal."
    },
    "removedTimestamp": {
      "type": "string",
      "format": "date-time",
      "description": "RFC 3339 timestamp when the membership removal occurred."
    },
    "details": {
      "type": "object",
      "additionalProperties": true,
      "description": "Provider-specific metadata for this membership event."
    }
  }
}